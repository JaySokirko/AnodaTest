<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NavViewImageStyle">
        <item name="android:layout_width">@dimen/nav_view_image_width</item>
        <item name="android:layout_height">@dimen/nav_view_image_height</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="NavViewRootViewStyle" parent="CardView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/nav_view_height</item>
        <item name="cardCornerRadius">0dp</item>
        <item name="cardElevation">@dimen/x2</item>
        <item name="cardBackgroundColor">@color/gray_50</item>
    </style>

</resources>